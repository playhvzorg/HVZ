@inject NavigationManager Navigation

@code {

    protected override void OnInitialized()
    {
        string uri = Navigation.ToBaseRelativePath(Navigation.Uri);
        Navigation.NavigateTo("Account/Login?returnUrl=/" +
                              uri, true);
    }

}