@inherits GameLogBase

<EventLogMessage 
    TimeStamp="@GameLog.Timestamp"
    Title="@EventInfoTitle"
    Users="@(new[] { GameLog.User })" />

@code {
    string EventInfoTitle => $"{GameLog.User.FullName} created {((GameCreatedLogData)GameLog).GameName}";
}
