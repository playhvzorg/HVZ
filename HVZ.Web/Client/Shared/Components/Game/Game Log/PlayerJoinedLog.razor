@inherits GameLogBase

<EventLogMessage
    TimeStamp="@GameLog.Timestamp"
    Users="@(new[] { GameLog.User })"
    Title="@Title"/>

@code {
    string Title => $"{GameLog.User.FullName} has joined the game";
}
